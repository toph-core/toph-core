import 'package:flutter/material.dart';

/// Kassir: kassa bo‘limi (placeholder).
class KassaTab extends StatelessWidget {
  const KassaTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Kassa'),
    );
  }
}
