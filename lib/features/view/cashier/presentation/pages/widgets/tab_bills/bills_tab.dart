import 'package:flutter/material.dart';

/// Kassir: hisob-fakturalar (placeholder).
class BillsTab extends StatelessWidget {
  const BillsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hisoblar'),
    );
  }
}
