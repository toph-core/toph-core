import 'package:flutter/material.dart';

/// Kassir: taomlar bo‘limi (placeholder).
class DishesTab extends StatelessWidget {
  const DishesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Taomlar'),
    );
  }
}
